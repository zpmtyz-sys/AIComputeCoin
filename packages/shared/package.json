{
  "name": "@computecoin/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared protobuf definitions and generated types for ComputeCoin",
  "scripts": {
    "proto:generate": "echo 'Proto generation configured per-service via build.rs (Rust) and protoc plugins'",
    "build": "tsc",
    "clean": "rm -rf dist generated"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
