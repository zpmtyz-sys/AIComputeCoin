[package]
name = "computecoin-chain"
version = "0.1.0"
edition = "2021"
description = "ComputeCoin blockchain logic - token, staking, governance, and consensus"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
sha2 = "0.10"
hex = "0.4"
ed25519-dalek = { version = "2", features = ["rand_core"] }
rand = "0.8"
rust_decimal = { version = "1", features = ["serde-with-str"] }
