apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-oracle-service
  labels:
    app: oracle-service
    app.kubernetes.io/name: oracle-service
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: {{ .Values.service.protocol }}
      name: grpc
  selector:
    app: oracle-service
    app.kubernetes.io/instance: {{ .Release.Name }}
